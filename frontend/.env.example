# When running locally with Vite proxy, leave this as /api
# When deploying to Vercel/Netlify pointing at a separate backend, set the full URL:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=/api
